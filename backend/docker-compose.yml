services:
  bomberman-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bomberman-server
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
